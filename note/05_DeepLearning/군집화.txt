정답(레이블)이 없는 데이터에서 비슷한 특성을 가진 데이터끼리 그룹으로 묶는 기법입니다.

K-Means 알고리즘:

사용자가 지정한 군집의 수(K)만큼 중심점(Centroid)을 무작위로 설정합니다.

모든 데이터는 가장 가까운 중심점에 할당됩니다.

각 군집의 중심점을 해당 군집에 속한 데이터들의 평균 위치로 이동시킵니다.

중심점이 더 이상 변하지 않을 때까지 위 과정을 반복합니다.

make_blobs: 군집화 연습을 위한 가상 데이터를 생성하는 데 사용됩니다.