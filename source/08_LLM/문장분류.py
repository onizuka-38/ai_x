import os
import pandas as pd
import random
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()
client = OpenAI(
  api_key=os.environ.get("OPENAI_API_KEY"),
)

df = pd.read_csv("data/samples.csv")
TEST_COUNT = 20
success = 0

for i,count in enumerate(range(TEST_COUNT)):
    random_index = random.randrange(1,16542)
    response = client.responses.create(
        model="gpt-4o",
        instructions="""[역할]
당신은 최고의 한국어 문장 분석 AI입니다. 당신의 유일한 임무는 사용자가 입력한 번호가 매겨진 각 문장을 ‘유형, 극성, 시제, 확실성’ 4가지 속성에 따라 분류하고, 지정된 형식으로만 결과를 출력하는 것입니다.

[분류 기준]
아래 기준과 예시를 바탕으로 각 문장을 신중하게 분석하고 가장 적합한 라벨 하나씩을 선택합니다. 모호하면 데이터 분포에 맞춘 기본값: 사실형,긍정,현재,확실을 사용합니다.

1. 유형

사실형: 사실·보고·발표·판결·현황 등 객관적 서술. “말했다/밝혔다/발표했다/판결이다” 같은 발화·결정 사실은 사실형.

추론형: 근거→결론·해석·평가·암시. 표지: 따라서/그러므로/~로 보아/~로 해석/~의 의미/관건/시사/눈길을 끈다/보여준다.

대화형: 질문·명령·요청·권유·감탄 등 발화 행위. 표지: ?, ~까/~나/~요?, 해라/해주세요/바란다/권한다/원한다, 과연/그렇다면/다만/그러나/무엇보다.
단, “A가 요청했다/밝혔다”는 요청 ‘사실’의 보고이므로 사실형.

예측형: 미래 전망·계획·예정·추정이 내용의 핵심일 때. 표지: 전망/예상/추정/가능/보인다/향후/~겠다/~할 것이다/예정/계획.

혼합이면 내용의 본질을 우선(예: “발표: 내년 증산 계획” → 예측형).

2. 극성

부정: 않다/못하다/없다/불가/어렵다/감소/하락/부진/악화/적자/손실/파기/취소/철회/폐쇄/중단/미달/미흡/위반/불법/과징금/징역/피해/사망/논란/우려 등.

긍정: 승인/인정/수상/합의/타결/복구/증가/성장/호조/개선/흑자/도입/출시/확대/강화/완공/개막/개최 등.

미정: 긍·부정 혼재 또는 판단 곤란.

3. 시제

미래: 예정/계획/추진/향후/내일/내년/~겠다/~할 것이다/~될 전망.

과거: ~했다/~였다/~됐다/지난/어제/작년/발표했다/판결했다/확인했다/결정했다/밝혔다/합의했다/하기로 했다(결정 행위).

현재: ~이다/~한다/있다/중이다/현재/지금/판결이다/결정이다/발표다.

세부 규칙

“~하기로 했다/결정했다/합의했다” → 확실. 시제는 과거(결정 사실에 초점) 표시가 있으면 과거, 그 외엔 실행 관점의 미래를 기본.

“예정/계획” → 기본 미래.

보도문 현재형(“판결이다/발표다/…이다”)은 현재.

4. 확실성

확실: 단정·공식 결정/판결/승인/인증/발표/통계/규정/확정/합의/계약/임명/선정/개최/출시.
“가능성/전망/예상”이라도 **“있다/높다/크다/확인됐다/열어뒀다”**처럼 사실 진술이면 확실로 본다.

불확실: 가정·추정·전망이 주축이거나 “~수 있다/~수도 있다/~듯/~보인다/가능/검토/논의/잠정”.

[출력 규칙 (매우 중요)]

입력 번호를 그대로 사용하여 한 줄에 한 문장만 출력합니다.

번호. 유형,극성,시제,확실성 순서로 작성합니다.

쉼표로만 구분하며 쉼표 앞뒤에 공백을 넣지 않습니다.

라벨은 정확히 다음 표기만 사용합니다:
유형=사실형/추론형/대화형/예측형, 극성=긍정/부정/미정, 시제=과거/현재/미래, 확실성=확실/불확실.

추가 설명·따옴표·괄호·줄임표 등 어떤 부가 텍스트도 금지합니다.

[학습 예시]

입력: 우리 공군 특수비행팀 블랙이글스는 이번에 축하비행을 한 영국 공군 레드 애로우즈와 깊은 인연을 가지고 있다.
출력: 사실형,긍정,현재,확실

입력: A씨는 여동생의 재산 내역을 C씨에게 인계도 제대로 하지 않았다.
출력: 사실형,부정,과거,확실

입력: 이 곳에서는 재무개선 추진과 이행 실적을 종합 관리할 예정이다.
출력: 사실형,긍정,미래,확실

입력: 납세자는 심의위원회에서 의견을 표명할 수 있고 과세 전 적부심사와 이의신청, 심판청구 등을 통해 이의를 제기할 수 있다.
출력: 추론형,긍정,미래,불확실

입력: 정부와 노사는 임금체계 개편을 추진하기로 했다.
출력: 사실형,긍정,미래,확실

입력: 과연 이런 조치가 실효성이 있을까?
출력: 대화형,미정,현재,불확실

입력: 전문가들은 내년 성장률이 2%대를 회복할 것으로 전망했다.
출력: 예측형,긍정,미래,불확실

입력: 경찰은 수사를 확대한다고 밝혔다.
출력: 사실형,긍정,과거,확실

입력: 국내 흥행 가능성은 높게 점쳐지고 있다.
출력: 사실형,긍정,현재,확실

입력: 사업 타당성이 낮아 계획은 재검토될 수 있다.
출력: 추론형,부정,미래,불확실""",
        input=df.iloc[random_index]['user_prompt'],
        temperature=0.4
    )
    answer = str(df.iloc[random_index,1])+','+str(df.iloc[random_index,2])+','+str(df.iloc[random_index,3])+','+str(df.iloc[random_index,4])
    if response.output_text.strip() != answer:
        print(f'{count+1}:',df.iloc[random_index]['user_prompt'])
        print('정답:',answer)
        print('GPT:',response.output_text)
    else:
        success += 1
    
print('정답률:',success/TEST_COUNT)